export const styles = theme => ({
	appBar: {
    zIndex: theme.zIndex.drawer + 1,
	},
	progress: {
    maxWidth: "50%"
  },
  screen: {
		width: "100vw",
		height: "100vh",
		display: "flex",
		alignItems: "center",
		justifyContent: "center"
  },
});